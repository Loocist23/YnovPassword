<Window x:Class="YnovPassword.CreateDossierWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Créer Dossier" Height="200" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Margin="5">Nom du dossier:</TextBlock>
        <TextBox x:Name="DossierNameTextBox" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <Button Grid.Row="2" Grid.ColumnSpan="2" Content="Créer" Margin="5" Click="CreateButton_Click"/>
    </Grid>
</Window>
